/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [], // Add any external domains if needed
    formats: ['image/webp'], // Changed from 'image/jpeg' to 'image/webp'
  },
}

module.exports = nextConfig 